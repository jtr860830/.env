# Powerlevel9k configuration
source ~/.config/env/Zshell/powerlevel9k.zsh

# Plugin manager
export ZPLUG_HOME=/usr/local/opt/zplug
source $ZPLUG_HOME/init.zsh

  # Plugins
  zplug "bhilburn/powerlevel9k", use:powerlevel9k.zsh-theme
  zplug "zdharma/fast-syntax-highlighting"
  zplug "zsh-users/zsh-autosuggestions"
  zplug "zsh-users/zsh-completions"
  zplug "zsh-users/zsh-history-substring-search"
  zplug "skywind3000/z.lua"
  zplug "hlissner/zsh-autopair", defer:2

  # Install plugins if there are plugins that have not been installed
  if ! zplug check --verbose; then
    printf "Install? [y/N]: "
    if read -q; then
      echo; zplug install
    fi
  fi

# Source plugins
zplug load --verbose
# End plugin manager

# Vi-mode
bindkey -v

# Command history
export HISTSIZE=10000
export SAVEHIST=10000
export HISTFILE=~/.backup/zhistory

# Z jump around
eval "$(lua $ZPLUG_HOME/repos/skywind3000/z.lua/z.lua --init zsh)"
export _ZL_DATA=~/.backup/z
export _ZL_MATCH_MODE=1

# zsh-history-substring-search
bindkey '^[[A' history-substring-search-up
bindkey '^[[B' history-substring-search-down

# zsh-autopair
autopair-init

# Command-line fuzzy finder
[ -f ~/.backup/fzf.zsh ] && source ~/.backup/fzf.zsh

# Source general environment
source ~/.config/env/profile
source ~/.config/env/Zshell/zstyle-completion
